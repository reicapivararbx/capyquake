{
  "name": "capiquake",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "server": "node server/index.js",
    "start": "concurrently \"npm run dev\" \"npm run server\"",
    "tunnel": "cloudflared tunnel --url http://localhost:8080",
    "world": "concurrently -k -s first \"npm run start\" \"npm run tunnel\""
  },
  "dependencies": {
    "three": "^0.164.0",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "vite": "^5.3.0"
  }
}
